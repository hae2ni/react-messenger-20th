export interface BoxPropType {
  user?: string;
  showProfile: boolean;
  isSameTime: boolean;
  lastMessage: boolean;
  message: string;
  time: string;
}

export interface MessageBoxPropType {
  showProfile: boolean;
  message: string;
}
